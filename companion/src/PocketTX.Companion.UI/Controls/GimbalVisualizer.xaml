<UserControl x:Class="PocketTX.Companion.UI.Controls.GimbalVisualizer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="180" d:DesignWidth="180">
    
    <Border Background="{DynamicResource CardHeaderBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="2"
            CornerRadius="12"
            Padding="8">
        <Grid x:Name="ContainerGrid" ClipToBounds="True">
            <!-- Crosshair Grid Lines -->
            <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Height="1" VerticalAlignment="Center"/>
            <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0" Width="1" HorizontalAlignment="Center"/>

            <!-- Center Circle -->
            <Ellipse Width="16" Height="16" Stroke="{DynamicResource BorderBrush}" StrokeThickness="1" HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <!-- Gimbal Stick Dot -->
            <Canvas>
                <Ellipse x:Name="StickDot"
                         Width="18" Height="18"
                         Fill="{DynamicResource PrimaryAccentBrush}"
                         Stroke="#FFFFFF"
                         StrokeThickness="2">
                    <Ellipse.Effect>
                        <DropShadowEffect Color="#5B8DEF" BlurRadius="8" ShadowDepth="0" Opacity="0.8"/>
                    </Ellipse.Effect>
                </Ellipse>
            </Canvas>
            
            <!-- Label -->
            <TextBlock x:Name="TitleLabel" Text="GIMBAL"
                       Foreground="{DynamicResource TextMutedBrush}"
                       FontSize="10" FontWeight="Bold"
                       HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4"/>
        </Grid>
    </Border>
</UserControl>
