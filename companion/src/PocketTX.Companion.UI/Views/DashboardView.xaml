<UserControl x:Class="PocketTX.Companion.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#0F1218">
        <StackPanel Margin="24">
            
            <!-- Top Summary Cards Grid -->
            <UniformGrid Columns="4" Margin="0,0,0,16">
                <!-- Connection Card -->
                <Border Background="#1B202E" BorderBrush="#2A3246" BorderThickness="1" CornerRadius="12" Padding="16" Margin="6">
                    <StackPanel>
                        <DockPanel>
                            <TextBlock Text="CONNECTION" Foreground="#8C9BB4" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                            <Button Command="{Binding ScanDevicesCommand}" Content="🔄 REFRESH" Padding="6,3" FontSize="9" FontWeight="Bold" Foreground="#5B8DEF" Background="#141822" BorderBrush="#2A3246" BorderThickness="1" HorizontalAlignment="Right"/>
                        </DockPanel>
                        <TextBlock Text="{Binding ConnectionStatusText}" Foreground="#F0F4FC" FontSize="15" FontWeight="SemiBold" Margin="0,8,0,4" TextWrapping="Wrap"/>
                        <Border Background="#22C55E" CornerRadius="10" Padding="8,2" HorizontalAlignment="Left">
                            <TextBlock Text="TEST MODE ACTIVE" Foreground="#FFFFFF" FontSize="10" FontWeight="Bold"/>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Active Profile Card -->
                <Border Background="#1B202E" BorderBrush="#2A3246" BorderThickness="1" CornerRadius="12" Padding="16" Margin="6">
                    <StackPanel>
                        <TextBlock Text="ACTIVE PROFILE" Foreground="#8C9BB4" FontSize="11" FontWeight="Bold"/>
                        <TextBlock Text="{Binding ActiveProfileName}" Foreground="#5B8DEF" FontSize="18" FontWeight="Bold" Margin="0,8,0,4" TextWrapping="Wrap"/>
                        <TextBlock Text="250Hz / 1000-2000us" Foreground="#8C9BB4" FontSize="11"/>
                    </StackPanel>
                </Border>

                <!-- Latency Card -->
                <Border Background="#1B202E" BorderBrush="#2A3246" BorderThickness="1" CornerRadius="12" Padding="16" Margin="6">
                    <StackPanel>
                        <TextBlock Text="LATENCY" Foreground="#8C9BB4" FontSize="11" FontWeight="Bold"/>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,4">
                            <TextBlock Text="{Binding LatencyMs}" Foreground="#F0F4FC" FontSize="24" FontWeight="Bold"/>
                            <TextBlock Text=" ms" Foreground="#8C9BB4" FontSize="14" VerticalAlignment="Bottom" Margin="4,0,0,3"/>
                        </StackPanel>
                        <TextBlock Text="100Hz Heartbeat" Foreground="#8C9BB4" FontSize="11"/>
                    </StackPanel>
                </Border>

                <!-- Simulator Status Card -->
                <Border Background="#1B202E" BorderBrush="#2A3246" BorderThickness="1" CornerRadius="12" Padding="16" Margin="6">
                    <StackPanel>
                        <TextBlock Text="SIMULATOR" Foreground="#8C9BB4" FontSize="11" FontWeight="Bold"/>
                        <TextBlock Text="{Binding SimulatorStatusText}" Foreground="#F0F4FC" FontSize="15" FontWeight="SemiBold" Margin="0,8,0,4" TextWrapping="Wrap"/>
                        <TextBlock Text="Auto-Detect Active" Foreground="#8C9BB4" FontSize="11"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- Middle Section: Latency Graph & System Summary -->
            <Grid Margin="6,0,6,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <!-- Latency Live Waveform -->
                <Border Grid.Column="0" Background="#1B202E" BorderBrush="#2A3246" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,12,0">
                    <DockPanel>
                        <TextBlock Text="REAL-TIME LATENCY MONITOR" Foreground="#8C9BB4" FontSize="11" FontWeight="Bold" DockPanel.Dock="Top" Margin="0,0,0,12"/>
                        <Border Height="140" Background="#141822" CornerRadius="8" Padding="16">
                            <TextBlock Text="Latency Ticker Running (100ms)" Foreground="#5B8DEF" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </DockPanel>
                </Border>

                <!-- Backend & Telemetry Summary Card -->
                <Border Grid.Column="1" Background="#1B202E" BorderBrush="#2A3246" BorderThickness="1" CornerRadius="12" Padding="16">
                    <StackPanel>
                        <TextBlock Text="VIRTUAL BACKEND" Foreground="#8C9BB4" FontSize="11" FontWeight="Bold"/>
                        <TextBlock Text="{Binding ActiveBackendText}" Foreground="#5B8DEF" FontSize="16" FontWeight="Bold" Margin="0,8,0,12" TextWrapping="Wrap"/>
                        
                        <Separator Background="#2A3246" Margin="0,0,0,12"/>

                        <Grid Margin="0,4">
                            <TextBlock Text="Update Frequency" Foreground="#8C9BB4"/>
                            <TextBlock Text="{Binding UpdateFrequencyHz, StringFormat='{}{0} Hz'}" Foreground="#F0F4FC" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <TextBlock Text="Packet Rate" Foreground="#8C9BB4"/>
                            <TextBlock Text="{Binding PacketRate, StringFormat='{}{0} p/s'}" Foreground="#F0F4FC" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>

                        <Grid Margin="0,4">
                            <TextBlock Text="UI Rendering FPS" Foreground="#8C9BB4"/>
                            <TextBlock Text="{Binding Fps, StringFormat='{}{0} FPS'}" Foreground="#F0F4FC" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Bottom Section: Quick Log Stream -->
            <Border Background="#1B202E" BorderBrush="#2A3246" BorderThickness="1" CornerRadius="12" Padding="16" Margin="6">
                <DockPanel>
                    <TextBlock Text="LIVE EVENT TERMINAL" Foreground="#8C9BB4" FontSize="11" FontWeight="Bold" DockPanel.Dock="Top" Margin="0,0,0,12"/>
                    <ItemsControl ItemsSource="{Binding QuickLogs}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" Foreground="#8C9BB4" FontFamily="Consolas" FontSize="12" Margin="0,2"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </DockPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
